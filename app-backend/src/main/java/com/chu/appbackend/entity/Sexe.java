package com.chu.appbackend.entity;

/**
 * Enumération représentant le sexe biologique du patient,
 * utilisée notamment dans les calculs de scores cliniques (NEWS2, ESI).
 */
public enum Sexe {
    HOMME,
    FEMME
}
